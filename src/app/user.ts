export class User {
    username: string;
    destination: string;
    note: number;
    constructor(
        username: string= '',
        destination: string= '',
        note: number= 0,
    ) {}
}
